{
  "name": "@graphite/render-svg",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic SVG renderer for Graphite",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "@graphite/diagram-spec": "*"
  }
}
